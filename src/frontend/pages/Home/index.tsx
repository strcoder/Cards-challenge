import React from 'react';
import Cards from '../../containers/Cards';
import './styles.scss';

const Home = () => {
  return (
    <section className='Home'>
      <Cards />
    </section>
  );
};

export default Home;
